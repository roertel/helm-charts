apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pytivo.fullname" . }}-test-connection"
  labels:
    {{- include "pytivo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pytivo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
