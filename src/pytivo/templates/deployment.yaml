apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pytivo.fullname" . }}
  labels:
    {{- include "pytivo.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "pytivo.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "pytivo.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "pytivo.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /usr/bin/python
            - /opt/pytivo/lucasnz/pyTivo.py
#            - --config
#            - /config/server.config
            {{- range $key, $val := (merge .Values.config .Values.secret) }}
            - --extraconf
            - {{ $key }}.conf
            {{- end }}
          ports:
            - name: broadcast
              containerPort: 2190
              protocol: UDP
            - name: management
              containerPort: {{ .Values.servicePort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: "{{ .Values.healthProbePath | default '/' }}"
              port: {{ .Values.servicePort }}
          readinessProbe:
            httpGet:
              path: "{{ .Values.healthProbePath | default '/' }}"
              port: {{ .Values.servicePort }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.mediaMount }}
            - name: {{ include "pytivo.fullname" . }}-media
              mountPath: /media
              readOnly: true
            {{- end }}
            {{- if .Values.config }}
            - name: {{ include "pytivo.fullname" . }}-config
              mountPath: /config
              readOnly: true
            {{- end }}
            {{- if .Values.secret }}
            - name: {{ include "pytivo.fullname" . }}-secret
              mountPath: /secret
              readOnly: true
            {{- end }}
      volumes:
        {{- if .Values.mediaMount }}
        - name: {{ include "pytivo.fullname" . }}-media
          {{- toYaml .Values.mediaMount | nindent 10 }}
        {{- end }}
        {{- if .Values.config }}
        - name: {{ include "pytivo.fullname" . }}-config
          configMap:
            name: {{ include "pytivo.fullname" . }}
        {{- end }}
        {{- if .Values.secret }}
        - name: {{ include "pytivo.fullname" . }}-secret
          secret:
            secretName: {{ include "pytivo.fullname" . }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
