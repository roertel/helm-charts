View your admin password with the following command:

    kubectl get secret -n {{.Release.Namespace}} {{ include "openldap.fullname" . }}-admin -o jsonpath='{.data.admin}'|base64 -d

View your configuration (RO) password with:

    kubectl get secret -n {{.Release.Namespace}} {{ include "openldap.fullname" . }}-admin -o jsonpath='{.data.config}'|base64 -d

{{ include "openldap.fullname" . }} is now set up!