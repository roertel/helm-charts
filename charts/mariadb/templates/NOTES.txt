View your admin password with the following command:

    kubectl get secret -n {{.Release.Namespace}} {{ include "mariadb.fullname" . }} -o jsonpath='{.data.admin}'|base64 -d

{{ include "mariadb.fullname" . }} is now set up!
