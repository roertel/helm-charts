apiVersion: v1
kind: Service
metadata:
  name: {{ include "e2guardian.fullname" . }}
  labels:
    {{- include "e2guardian.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "e2guardian.selectorLabels" . | nindent 4 }}
