apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "e2guardian.fullname" . }}-test-connection"
  labels:
    {{- include "e2guardian.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "e2guardian.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
